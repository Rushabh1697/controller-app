{
  "name": "website",
  "version": "1.0.0",
  "description": "GyroPad Official Website",
  "main": "app.js",
  "scripts": {
    "build": "esbuild agentation-init.jsx --bundle --minify --outfile=agentation.js --define:process.env.NODE_ENV=\\\"production\\\"",
    "build:agentation": "esbuild agentation-init.jsx --bundle --minify --outfile=agentation.js --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "type": "commonjs",
  "devDependencies": {
    "agentation": "^3.0.2",
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  }
}
